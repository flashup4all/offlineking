export const url = 'http://localhost:8000/api/v1';//'https://tfap.herokuapp.com/api/tfap';//
export const img_url = 'http://localhost:8000/app/';//'https://tfap.herokuapp.com/api/tfap';//

/*export const header = (token) => {
    return {
        "Authorization": `Bearer ${token}`,
        "Content-Type": "application/json"
    }
};

*/